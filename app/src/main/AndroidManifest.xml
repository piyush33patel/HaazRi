<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.navigationhaazrai">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DateWise"
            android:label="DateWise Attendance"
            android:parentActivityName=".Home"/>
        <activity
            android:name=".Updates"
            android:label="Updates"
            android:parentActivityName=".Home" />
        <activity
            android:name=".SubjectWise"
            android:parentActivityName=".Home" />
        <activity
            android:name=".Account"
            android:label="Account"
            android:parentActivityName=".Home" />
        <activity
            android:name=".WorkInProgress"
            android:parentActivityName=".Home" />
        <activity
            android:name=".NewAccountLogin"
            android:label="Create New Account"
            android:parentActivityName=".Home" />
        <activity
            android:name=".Home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>